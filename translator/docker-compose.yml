version: '3.8'

services:
  # =========================================
  # SUBTITLE TRANSLATOR (Standalone)
  # =========================================
  subtitle-translator:
    image: ghcr.io/ghislainsamy/subtitle-extractor-translator/translator:latest
    container_name: subtitle-translator
    volumes:
      # Multi-folders: monter tous les dossiers à traiter
      - /mnt/nas/media/films:/media/movies
      - /mnt/nas/media/series:/media/series
      - /mnt/nas/media/documentaires:/media/documentaries
      
      # Volume pour les logs (optionnel)
      - /docker/subtitle-extractor-translator/translator/logs:/app/logs
    environment:
      # 🆕 Multi-Folders Support
      # Configuration JSON array avec les 3 dossiers
      - SOURCE_FOLDERS=["/media/movies", "/media/series", "/media/documentaries"]
      
      # Mode agent continu
      - WATCH_MODE=true
      
      # Intervalle de vérification (secondes)
      # 3600 = 1h, 21600 = 6h, 86400 = 24h
      - WATCH_INTERVAL=3600
      
      # 📝 Logs activés avec rotation automatique
      - LOG_FILE=/app/logs/translator.log
      - LOG_FILE_MAX_SIZE_MB=10        # Taille max par fichier (MB)
      - LOG_FILE_BACKUP_COUNT=2        # Nombre de backups (total = 3 fichiers)
      
      # ⚡ Performance optimisée : ~18 minutes pour 1945 lignes
      - PAUSE_SECONDS=10
      - BATCH_SIZE=50
      
      # 🔑 Clés API Gemini (créer sur https://aistudio.google.com/app/apikey)
      # IMPORTANT: Remplacer par vos vraies clés !
      - GEMINI_API_KEYS=["votre-clé-1", "votre-clé-2", "votre-clé-3"]
      
      # 🤖 Modèles Gemini avec quotas INDÉPENDANTS
      # Flash 2.0 (15 RPM, 1000 RPD) + Flash 1.5 (10 RPM, 250 RPD)
      # = 25 RPM total par clé, 1250 RPD par clé
      # Avec 3 clés = 75 RPM, 3750 RPD (capacité ~96 films/jour)
      - GEMINI_MODELS=["gemini-3-flash-preview", "gemini-2.5-flash"]

      # ⏰ Cooldown si erreur API (secondes)
      - COOLDOWN_SECONDS=3600
      
      # 🗑️ Variables de nettoyage (défaut: false = on garde tout)
      # Mettre à true pour activer le nettoyage automatique en production
      
      # Supprimer .fr.progress.json après traduction ?
      - DELETE_PROGRESS_AFTER=false
      
      # Supprimer .en.XXX.tmp après traduction ?
      - DELETE_SOURCE_AFTER=false
      
      # Supprimer .to.srt.tmp après traduction ?
      - DELETE_CONVERTED_AFTER=false
      
    restart: unless-stopped